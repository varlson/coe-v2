import React, { ReactNode } from "react";

function InfoCard({
  title,
  src,
  children,
}: {
  title: string;
  src: string;
  children: ReactNode;
}) {
  return <div></div>;
}

export default InfoCard;
